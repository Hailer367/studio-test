// This is a basic service worker file.
// It's required for PWA installability.

self.addEventListener('fetch', (event) => {
  // We are not caching anything for now.
  // This is just to make the app installable.
});
